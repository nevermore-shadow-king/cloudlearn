server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      host: http://localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}